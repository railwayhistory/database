path: news
content:
    en:
        title: News
        body: |
            {% load pagetags %}
            {% tagindex news %}
            {% for page in pages %}
            {{ page.local_content.title }}

            {{ page.local_content.body|safe }}        
            {% endfor %}
            {% endtagindex %}
